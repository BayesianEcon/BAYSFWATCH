% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GenData.R
\name{GenData}
\alias{GenData}
\title{Generate Simulation Data for Bayesian Factor Analysis}
\usage{
GenData(outv, outc, outn, Rcn = 1, Rrn = 1)
}
\arguments{
\item{outv}{A scalar value representing the amount to add as outliers.}

\item{outc}{An integer (1 to 4) defining the type of outlier injection:
\itemize{
\item 1: Adds \code{outv} to all values in the matrix.
\item 2: Adds \code{outv} to a single element (1,1) in the matrix.
\item 3: Adds \code{outv} to \code{outn} randomly chosen elements in the matrix.
\item 4: Adds \code{outv} to \code{Rcn} columns and \code{Rrn} rows, randomly selected.
}}

\item{outn}{The number of outliers to inject (only used in option 3). Must be lower than \code{p * n = 35}.}

\item{Rcn}{The number of columns to contain outliers (used in option 4). Default is 1.}

\item{Rrn}{The number of rows to contain outliers (used in option 4). Default is 1.}
}
\value{
A list containing:
\itemize{
\item \code{X3D0}: The generated 3D dataset without outliers.
\item \code{X3D}: The 3D dataset with outliers.
\item \code{M}: The mean matrix.
\item \code{Sl}: The row covariance matrix.
\item \code{Sp}: The scaled row covariance matrix.
\item \code{V}: The column covariance matrix.
}
}
\description{
This function generates a 3D dataset (5x7x100) for Bayes Factor (BF) outlier detection by simulating matrix-variate normal distributions and injecting outliers based on different scenarios.
}
\details{
The function first generates a dataset (a 5x7x100 array) from a matrix-variate normal distribution using \code{mvtnorm::rmvnorm}.
An outlier matrix is injected at the 80th position.
Depending on \code{outc}, the function injects outliers in different ways, modifying elements at specific positions or randomly distributing them.
The mean matrix is generated randomly and the factor controlling variance reduction in the prior is set to 0.01.
}
\examples{
set.seed(123)
data <- GenData(outv = 1.5, outc = 3, outn = 10, outp = 80)

}
